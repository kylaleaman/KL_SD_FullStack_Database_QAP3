<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search and Update Menu Item</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
</head>
<body>
    <h2>Update Menu Item</h2>
    <form action="/menu/update/<%= menuItem.id %>" method="POST" id="updateForm">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" value="<%= menuItem.name %>" required><br>
        <label for="description">Description:</label>
        <input type="text" id="description" name="description" value="<%= menuItem.description %>" required><br>
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" value="<%= menuItem.price %>" required><br>
        <label for="availability">Availability:</label>
        <input type="checkbox" id="availability" name="availability" <% if (menuItem.availability) { %> checked <% } %>><br>
        <button type="submit">Update</button>
    </form>
</body>
</html>



