<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Menu Item</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
</head>
<body>
    <h1>Delete Menu Item</h1>

    <!--Form for searching item-->
    <form action="/menu/delete" method="GET">
        <label for="itemId">Enter Item ID:</label>
        <input type="text" id="itemID" name="itemID" required>
        <button type="submit">Search</button>
    </form>

    <!--Delete confirmation-->
    <% if(menuItem) { %>
    <p>Are you sure you want to delete <%= menuItem.name %>?</p>
    <form action="/api/menu/<%= menuItem.id %>" method="POST">
        <button type="submit">Delete Item</button>
    </form>
    <% } else { %>
        <p>Item not found - incorrect item ID</p>
    <% } %>
</body>
</html>
